# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "dab_project_sunny"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-56f60291-144c.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-56f60291-144c.cloud.databricks.com
      # We always use /Users/matias.bertuzzi@sunnydata.ai for all resources to make sure we only have a single copy.
      root_path: /Users/matias.bertuzzi@sunnydata.ai/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as matias.bertuzzi@sunnydata.ai in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: matias.bertuzzi@sunnydata.ai
